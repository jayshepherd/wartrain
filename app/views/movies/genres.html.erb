<%= link_to 'All', {:controller => :movies, :action => :index}, {:class => 'bottom_links'} %>
<% @genres.each do |genre| %>
	<%= link_to genre.name, {:controller => :movies, :action => :index, :genre => genre.id},
	    {:class => 'bottom_links'} %>
<% end %>